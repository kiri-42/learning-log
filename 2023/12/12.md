# やったこと
- 履歴書用の写真撮影
- `go-algo-challenges`用の開発環境を整備
  - テスト用のCI環境を構築
  - `docker compose`でコンテナ環境を構築
  - 簡単にコマンドを実行できるように`Makefile`を作成

# 学んだこと

## `go-algo-challenges`用の開発環境を整備

### テスト用のCI環境を構築
CI環境の構築では、ブランチ名を使えば入力パラメータを指定できることを学んだ(ちょっと力技感あるけど)。

### 簡単にコマンドを実行できるように`Makefile`を作成
久しぶりに`Makefile`触ったけど、簡単に記述できて実行も楽になったので導入してよかった。数年前に使ったときは全然使い方わからなかったけど今は簡単に使えたので成長を感じた。
